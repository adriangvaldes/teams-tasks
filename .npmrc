# As configuracoes de instalacao deste monorepo (nodeLinker, allowlist de build
# scripts, politica de release age) ficam no pnpm-workspace.yaml: a partir do
# pnpm 11 e de la que elas sao lidas, nao mais do .npmrc.
